{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Property names are schema data, not identifiers, so they can carry characters that are not safe to drop into generated Go source. Interpolated raw, a quote or backslash ends the string literal early and the file does not compile; a percent sign is read by fmt as a verb.",
  "properties": {
    "a\"b": {
      "type": "string"
    },
    "back\\slash": {
      "type": "string"
    },
    "pct%s": {
      "type": "string"
    },
    "ordinary": {
      "type": "string"
    },
    "has`tick": {
      "type": "string"
    },
    "arr\"pct%s": {
      "description": "An array whose name carries both a quote and a percent. The length check keeps the name as an argument rather than splicing it into the format string, so the percent is never read as a verb and only Go-literal escaping applies.",
      "type": "array",
      "items": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1
      },
      "minItems": 1
    }
  },
  "required": [
    "a\"b",
    "back\\slash",
    "pct%s",
    "ordinary",
    "has`tick",
    "arr\"pct%s"
  ]
}
