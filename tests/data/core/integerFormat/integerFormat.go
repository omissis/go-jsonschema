// Code generated by github.com/atombender/go-jsonschema, DO NOT EDIT.

package test

import "encoding/json"
import "fmt"
import yaml "gopkg.in/yaml.v3"

type IntegerFormat struct {
	// Without --min-sized-ints the declared format wins outright; bounds only narrow
	// the type when that flag is set.
	BoundedWithFormat *int64 `json:"boundedWithFormat,omitempty,omitzero" yaml:"boundedWithFormat,omitempty" mapstructure:"boundedWithFormat,omitempty"`

	// `format: int32` pins the width at 32 bits on every platform.
	Count *int32 `json:"count,omitempty,omitzero" yaml:"count,omitempty" mapstructure:"count,omitempty"`

	// No format declared, so the default `int` is unchanged.
	NoFormat *int `json:"noFormat,omitempty,omitzero" yaml:"noFormat,omitempty" mapstructure:"noFormat,omitempty"`

	// An unrecognised integer format must not silently change the type; it falls
	// through to the default.
	UnknownFormat *int `json:"unknownFormat,omitempty,omitzero" yaml:"unknownFormat,omitempty" mapstructure:"unknownFormat,omitempty"`

	// `format: int64` must carry 64 bits. A bare `int` is 32 bits on 32-bit
	// platforms, where encoding/json and yaml.v3 both refuse a value this schema
	// declares valid — so the generated code cannot parse its own documents there.
	Version *int64 `json:"version,omitempty,omitzero" yaml:"version,omitempty" mapstructure:"version,omitempty"`
}

// UnmarshalJSON implements json.Unmarshaler.
func (j *IntegerFormat) UnmarshalJSON(value []byte) error {
	type Plain IntegerFormat
	var plain Plain
	if err := json.Unmarshal(value, &plain); err != nil {
		return err
	}
	if plain.BoundedWithFormat != nil && 100 < *plain.BoundedWithFormat {
		return fmt.Errorf("field %s: must be <= %v", "boundedWithFormat", 100)
	}
	if plain.BoundedWithFormat != nil && 0 > *plain.BoundedWithFormat {
		return fmt.Errorf("field %s: must be >= %v", "boundedWithFormat", 0)
	}
	*j = IntegerFormat(plain)
	return nil
}

// UnmarshalYAML implements yaml.Unmarshaler.
func (j *IntegerFormat) UnmarshalYAML(value *yaml.Node) error {
	type Plain IntegerFormat
	var plain Plain
	if err := value.Decode(&plain); err != nil {
		return err
	}
	if plain.BoundedWithFormat != nil && 100 < *plain.BoundedWithFormat {
		return fmt.Errorf("field %s: must be <= %v", "boundedWithFormat", 100)
	}
	if plain.BoundedWithFormat != nil && 0 > *plain.BoundedWithFormat {
		return fmt.Errorf("field %s: must be >= %v", "boundedWithFormat", 0)
	}
	*j = IntegerFormat(plain)
	return nil
}
