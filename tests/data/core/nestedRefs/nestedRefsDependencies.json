{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "definitions": {
    "Holder": {
      "type": "object",
      "properties": { "flag": { "type": "string" } },
      "dependencies": {
        "needsFlag": {
          "type": "object",
          "properties": { "extra": { "type": "string" } }
        }
      }
    }
  },
  "properties": {
    "viaDependencies": {
      "description": "Draft-07 `dependencies` is dual-form; the schema form parses into DependentSchemas, so a $ref descending through it resolves there. A property-name dependency lands in DependentRequired instead, is not a schema, and correctly finds nothing.",
      "$ref": "#/definitions/Holder/dependencies/needsFlag"
    }
  }
}
